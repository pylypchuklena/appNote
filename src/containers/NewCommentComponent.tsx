// import * as actions from '../actions/';
// import { AppState, SourceTypes } from '../types/NoteModel';
// import { connect, Dispatch } from 'react-redux';
// import * as constants from '../constants';
// import noteList from '../components/ListNotes'
// import { NotesCommentsPanel } from "../components/NotesComponent";


// export function mapStateToProps(state:AppState){
// //     var item = state.noteList.filter(item=> {return item.isSelected === true});
// //       var selectedItem = item.length>0 ? item[0] : undefined;
// //     return {
// //     selectedItem: selectedItem
// //   };
// }

// // export function mapDispatchToProps(dispatch: Dispatch<actions.INoteAction>) {
// //   return {
// //     onSelectionChanged: (id:number) => dispatch(actions.selectNote(id))
// //   };
// // }

// export default connect(mapStateToProps)(NotesCommentsPanel);